{% extends 'MarcoshoyaMarquejogoBundle:Provider:index.html.twig' %}

{% block sidebar %}
    {{ render(controller('MarcoshoyaMarquejogoBundle:Provider/Dashboard:sidebar', {view : 'dashboard', item: 'dashboard'})) }}
{% endblock %}

{% block container %}

    <div class="row">
        <div class="col-lg-12">
            <h1>Últimas reservas</h1>
            <div class="panel panel-default">
                <div class="panel-heading">
                    Quadras
                </div>        
                <!-- /.panel-heading -->
                <div class="panel-body">
                    <table class="table table-bordered">
                        <thead>
                            <tr>
                                <th width="6%">#</th>
                                <th width="12%">Status</th>
                                <th width="12%">Cliente</th>
                                <th>Data</th>
                                <th>Hora</th>
                                <th width="12%">Valor</th>                                
                                <th>Ações</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for entity in entities %}
                                <tr>
                                    <td>{{ entity.id }}</td>
                                    <td>{{ entity.status }}</td>
                                    <td>{{ entity.customer.name }}</td>
                                    <td>{{ entity.date.format('d-m-Y') }}</td>
                                    <td>{{ entity.date.format('H:i') }}</td>
                                    <td>{{ entity.totalPrice }}</td>
                                    <td>Visualizar </td>
                                </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
    <!-- /.row -->

{% endblock %}